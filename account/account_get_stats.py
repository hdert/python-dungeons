"""
Author: Justin
Description: see account_get_stats.__doc__
"""


def account_get_stats(conn, c):
  """Get the stats of an account and return them.

  Return values:
  user_id -- the user's id
  password -- the hash of the user's password
  room -- the room the user's in
  questions -- the questions the user hasn't attempted
  score -- the score of the user so far for each level
  leaderboard -- a list of all the user's leaderboard entries that has lists inside for each value inside each entry
  """


if __name__ == "__main__":
  account_get_stats()
